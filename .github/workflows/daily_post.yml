name: AI Football Auto-Post

on:
  schedule:
    # Runs at 08:00, 11:00, and 14:00 UTC only.
    # This covers morning predictions and pre-match updates.
    # No more late-night runs = no finished games.
    - cron: '0 8,11,14 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  post_news:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10' 

      - name: Install Dependencies
        run: pip install -r requirements.txt

      - name: Run AI Bot
        env:
          FB_PAGE_ACCESS_TOKEN: ${{ secrets.FB_PAGE_ACCESS_TOKEN }}
          FB_PAGE_ID: ${{ secrets.FB_PAGE_ID }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python main.py

      - name: Save History to Repo
        run: |
          git config --global user.name 'AI Bot'
          git config --global user.email 'bot@noreply.github.com'
          if [[ -n $(git status -s history.json) ]]; then
            git add history.json
            git commit -m "Update history"
            git push
          else
            echo "No changes to history."
          fi
